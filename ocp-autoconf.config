

(*************************************)
(*   Never edit options files while  *)
(*       the program is running      *)
(*************************************)
(* SECTION : Header                  *)
(* These options must be read first  *)
(*************************************)



(* Minimal version of OCaml *)
ocaml_minimal_version = "4.00.0"

(* Does the project need ocamllex *)
need_ocamllex = false

(* Does the project need ocamlyacc *)
need_ocamlyacc = false

(* Does the project need ocamlbuild *)
need_ocamlbuild = false

(* Does the project need ocamlfind *)
need_ocamlfind = true

(* Does the project need camlp4 *)
need_camlp4 = false

(* Packages needed by the project *)
need_packages = [
]

(* Packages that could be used by the project *)
optional_packages = [
]

(* Modules needed by the project *)
need_modules = [
]

(* Extra files to be substituted *)
extra_config_files = [
]

(* Extra variables to be substituted *)
extra_config_vars = [
]
project = {

(* Project Copyright *)
  copyright = "Copyright 2016 OCamlPro SAS"

(* Project Version *)
  version = "1.1"

(* Project Name *)
  name = "typerex-attic"
}
